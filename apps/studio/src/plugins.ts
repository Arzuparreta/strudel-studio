/**
 * Studio plugin load list (v1.0).
 *
 * Import each plugin so its side effects run (e.g. registerPluginTransform).
 * To add a plugin: add the import below and add the package to studio's dependencies.
 */
import "@strudel-studio/plugin-euclidean";
